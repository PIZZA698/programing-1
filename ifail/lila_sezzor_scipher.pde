String message = "message yay";
int key  = 2;
String encrypted = "  ";

void setup(){
  size (400,200);
  background (#1025DE);
    text ("orginial:"+ message, 70,20);
    text ("encrypted:"+ encrypted, 100,20);
  encrypyed = encrypt(key, message);
}

String encrypt(int shift, string text){
String result= "   ";    
for (int i=O; i < text.size(); i++){
  char letter = text.charAt(i);
  letter + shift';
letter += letter';



  
  
